const object = {
    name: "Joël",
    campus: "Remote",
}

const myInformation = () =>{
    return (`Hi my name is ${object.name} and I'm a student from the promotion ${object.campus}`)
}
module.exports = myInformation;